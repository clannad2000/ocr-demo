# AGENTS.md

## 适用范围

本文件适用于`ocr-demo/`及子目录。README记录运行方法，DESIGN记录系统设计；
不要把临时命令或单次批次详情复制到本文件。

## 操作权限

- 默认只修改代码、配置模板和文档，并执行本地语法、单元、配置和mock检查。
- 真实OCR、翻译、外部模型复核和批处理由用户运行，除非当前请求明确授权。
- 修改后提供精确命令，等用户运行，然后只读检查产物。
- 若缺少软件、依赖或需要管理员权限，立即停止，给出多个方案及优缺点，等用户选择。

## 安全与不可变数据
- Codex裁决不得写回页面JSON、`manifest.json`或`study.html`；只生成独立裁决和`study-reviewed.html`。
- 应用裁决前必须验证输入SHA-256和全部`page + region id`。

## 架构不变式

- `api_keys`、`model_profiles`和`model_usage`必须解耦；切换模型只改用途映射。
- Qwen思考开关使用`enable_thinking`，DeepSeek使用`thinking.type`。
- 主OCR是英文权威来源；坐标OCR、图像复核和文本复核仅产生证据或候选。
- 不用多数票自动合并。Codex必须读原英文、当前译文、上下文和术语表后直接裁决。
- 人工复核只用于Codex补充必要证据后仍无法决定的项目；清单可以为空。
- PDF回填只能读取锁定的最终译文快照；回填代码和AI排版规则不得自行改译文。
- PDF回填全局规则、模型资料和模型用途必须解耦；局部人工修正只写独立覆盖文件。
- 回填后的顺序是程序全量检查、AI可选抽查、人工视觉验收；AI不得重复程序检查或主动全章视觉复核。

## 学习译文准则

- 对象是小学生：数学正确性优先，之后是可理解性、自然中文和全章一致性。
- 可做不改变含义的意译，不得改变数字、条件、公式或规则。
- 拟声词、独立页码、纯数字表、纯公式和独立边长默认不翻译；嵌入自然语言的数字和单位必须保留。
- 额外教学说明放入`child_note`，不混入`final_translation`。

## 验证要求

- 代码改动至少运行`python3 -m py_compile ocr_demo.py`和`python3 -m unittest discover -s tests`。
- 配置改动需解析`ocr_config.example.json`；不得打印实际`ocr_config.json`密钥。
- 裁决改动需验证候选并集、决定计数、坐标唯一性、输入哈希和原记录不变。
- 必须区分语法/mock/本地验证与真实外部API端到端验证。
- PDF程序检查通过只能标记`program_checked`，不得表述为视觉验收通过；最终视觉接受由用户确认。
