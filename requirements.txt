# Python packages
# The OCR and translation pipeline otherwise uses only the Python standard library.
# Required by English erasure, finalization, and Chinese PDF writing.
PyMuPDF>=1.23.0,<2.0.0
numpy>=1.26.0,<3.0.0
opencv-python-headless>=4.9.0,<5.0.0

# System dependencies (install outside pip; these are not Python packages):
# - Poppler: provides `pdftoppm` and `pdfinfo`, required for PDF page rendering
#   and page counting in the OCR pipeline.
# - Node.js >=18: required by the DeepL MCP bridge. Install its locked project
#   dependency with `npm install` (see package-lock.json).
